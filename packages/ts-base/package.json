{
  "name": "@ts-mono/ts-base",
  "version": "0.0.0",
  "bin": {
    "init-ts-base": "bin/initTSBase"
  },
  "main": "index.js",
  "lint-staged": {},
  "dependencies": {
    "fast-glob": "3.2.11",
    "fs-extra": "10.0.1",
    "picocolors": "1.0.0"
  },
  "scripts": {
    "build": "rm -rf out && tsc -b --force && ts-node -T scripts/build",
    "deploy": "yarn build && yarn ts-node ../../scripts/deployDirBranch",
    "prepare": ""
  }
}
