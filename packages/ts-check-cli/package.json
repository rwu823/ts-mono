{
  "name": "ts-check-cli",
  "version": "0.0.1",
  "sideEffects": false,
  "bin": {
    "ts-check": "bin/ts-check"
  },
  "main": "main",
  "dependencies": {
    "ora": "6.0.0"
  },
  "scripts": {
    "build": "tsc -b && cp -r package.json bin out/"
  }
}
