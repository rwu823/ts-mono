{
  "name": "ts-check-cli",
  "version": "0.0.1",
  "sideEffects": false,
  "bin": {
    "ts-check": "bin/ts-check"
  },
  "dependencies": {
    "ora": "4.1.0"
  },
  "scripts": {
    "build": "tsc -b && cp -r package.json bin out/"
  }
}
